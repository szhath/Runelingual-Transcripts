# RuneLingual zh_cn Setup Dashboard

Open in browser:

```bash
open ~/.openclaw/workspace/Runelingual-Transcripts/dashboard/zh_cn-setup-dashboard.html
```

This dashboard explains:
- no-lag architecture (prebuilt files, no runtime translation)
- current progress
- exact next commands to run
- where all zh_cn files live
